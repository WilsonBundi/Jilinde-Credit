function TestRegistration() {
  return (
    <div style={{
      minHeight: '100vh',
      background: 'linear-gradient(135deg, #4caf50 0%, #2e7d32 100%)',
      display: 'flex',
      alignItems: 'center',
      justifyContent: 'center',
      color: 'white',
      fontSize: '24px',
      fontWeight: 'bold',
      textAlign: 'center'
    }}>
      <div>
        <h1>🎉 TEST REGISTRATION PAGE WORKING!</h1>
        <p>This is a simple test to verify routing is working.</p>
        <p>If you can see this, the navigation is successful.</p>
      </div>
    </div>
  );
}

export default TestRegistration;